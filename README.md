# Shiori Blog

一个极简主义的静态博客生成器，专为 GitHub Pages 设计。

## 特性

- 📝 Markdown 写作，支持 frontmatter
- 🎨 极简主义设计，专注阅读体验
- ⚡ 快速构建，秒级生成
- 📱 响应式设计，完美适配移动端
- 🏷️ 支持分类和标签
- 🔍 代码高亮（highlight.js）
- 📄 自动生成归档页
- ⭐ 支持推荐文章功能
- 👤 个人信息展示（头像、简介）
- 🎯 三栏首页布局（主内容 + 侧边栏）

## 快速开始

### 安装依赖

```bash
npm install
```

### 配置

编辑 `shiori.config.json`:

```json
{
  "site": {
    "title": "Your Blog Title",
    "subtitle": "Your subtitle",
    "author": "Your Name",
    "avatar": "https://example.com/avatar.png",
    "bio": "Your bio description"
  },
  "featured": {
    "limit": 5,
    "show_on_homepage": true
  }
}
```

### 写文章

在 `source/_posts/` 目录下创建 Markdown 文件:

```markdown
---
title: Your Post Title
date: 2025-12-26 10:00
categories:
  - Technology
tags:
  - tutorial
  - guide
featured: true
---

# Your Content

Write your post here...
```

**支持的 Frontmatter 字段：**

- `title`: 文章标题
- `date`: 发布日期
- `updated`: 更新日期（可选）
- `author`: 作者（可选，默认使用配置文件中的作者）
- `categories`: 分类列表
- `tags`: 标签列表
- `featured`: 是否为推荐文章（默认 false）
- `excerpt`: 摘要（可选，也可以使用 `<!-- more -->` 分隔符）
- `published`: 是否发布（默认 true）

### 本地预览

```bash
npm run serve
```

访问 http://localhost:4000

### 构建

```bash
npm run build
```

生成的静态文件在 `dist/` 目录。

## 部署到 GitHub Pages

1. 将代码推送到 GitHub 仓库
2. 在仓库设置中启用 GitHub Pages
3. 选择 GitHub Actions 作为部署源
4. 推送代码自动触发部署

配置已包含在 `.github/workflows/deploy.yml`。

## 项目结构

```
shiori-blog/
├── lib/                    # 核心库文件
│   ├── post-parser.js     # 文章解析器
│   ├── template-engine.js # 模板引擎
│   ├── shiori.js          # 主生成器
│   ├── config.js          # 配置加载器
│   └── asset-copier.js    # 资源复制器
├── themes/                # 主题文件
│   └── minimalist/
│       └── assets/css/
│           └── style.css  # 样式文件
├── source/                # 源文件目录
│   ├── _posts/           # Markdown 文章
│   ├── categories/       # 分类页面
│   └── tags/             # 标签页面
├── shiori.config.json    # 配置文件
├── package.json          # 依赖管理
└── cli.js               # 命令行工具
```

## 自定义主题

编辑 `themes/minimalist/assets/css/style.css`，使用 CSS 变量轻松定制:

```css
:root {
  --bg-primary: #FAFAF9;
  --text-primary: #1C1917;
  --accent: #292524;
  /* ... 更多变量 */
}
```

## 推荐文章功能

在文章的 frontmatter 中添加 `featured: true`:

```markdown
---
title: My Featured Post
featured: true
---
```

首页会自动在侧边栏显示推荐文章列表。推荐文章的数量可以在配置文件中通过 `featured.limit` 设置。

## 常用命令

```bash
# 创建新文章
npm run new "文章标题"

# 本地预览（支持热重载）
npm run serve

# 构建博客
npm run build

# 预览构建结果
npm run preview
```

## 首页布局说明

博客首页采用现代化的三栏布局：

- **左侧（主内容区）**：显示文章列表，按时间倒序排列
- **右上（侧边栏）**：个人信息卡片，显示头像、作者名和简介
- **右下（侧边栏）**：推荐文章列表，显示标记为 featured 的文章

在移动端（屏幕宽度 < 1024px），侧边栏会自动移动到内容上方，确保良好的阅读体验。

## 配置选项

### 站点配置（site）

- `title`: 站点标题
- `subtitle`: 副标题
- `description`: 描述
- `author`: 作者名
- `avatar`: 头像 URL
- `bio`: 个人简介
- `language`: 语言代码
- `timezone`: 时区

### 推荐文章配置（featured）

- `limit`: 推荐文章数量限制（默认 5）
- `show_on_homepage`: 是否在首页显示（默认 true）

### 分页配置（pagination）

- `posts_per_page`: 每页文章数（默认 10）
- `path`: 分页路径（默认 "page"）

### 文章配置（posts）

- `default_layout`: 默认布局（默认 "post"）
- `excerpt_length`: 摘要长度（默认 200）
- `show_excerpt`: 是否显示摘要（默认 true）
- `date_format`: 日期格式（默认 "YYYY-MM-DD"）
- `show_reading_time`: 是否显示阅读时间（默认 true）

## 许可证

MIT License
